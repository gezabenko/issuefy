<div class="form">
 <fieldset>
  <h1><%= l(:issuefy_title) %></h1>

  <p class="subtitle"> <%= l(:issuefy_desc) %> </p>

  <%= form_for :file_upload, :html => {:multipart => true} do |f| %>
    <p>
      <%= radio_button_tag(:issuefy_file_type, "issues", true) %> <%= label_tag(:issuefy_file_type, "Issues") %><br/>
      <%= radio_button_tag(:issuefy_file_type, "times") %> <%= label_tag(:issuefy_file_type, "Time entries") %><br/>
    </p>
    <%= f.file_field :my_file %>
    <%= f.submit l(:issuefy_action) %>
  <% end %>
 </fieldset>
</div>
<br/><br/><br/>
<div class="wiki">
  <h2>Help</h2>
  <p>
    <ul>
      <li>The table contains only the necessary data, the headers and footers will not be able to interpret.</li>
      <li>The date format is little-endian with full year numbers.</li>
    </ul>
  </p>
  <h3>Issues</h3>
  <p>
    Create issues from spreadsheet file. Columns:
  </p>
  <p>
    <table>
      <tbody>
        <tr>
          <td>Tracker name (<b>mandatory</b>)</td>
          <td>Assign login or group name</td>
          <td>Subject (<b>mandatory</b>)</td>
          <td>Description</td>
          <td>Start date (<i>dd/mm/yyyy</i>)</td>
          <td>Due date (<i>dd/mm/yyyy</i>)</td>
          <td>Estimated time</td>
          <td>Parent issue (id or name)</td>
        </tr>
      </tbody>
    </table>
  </p>
  <h3>Time entries</h3>
  <p>
    Create time entries from spreadsheet file. Columns:
  </p>
  <p>
    <table>
      <tbody>
        <tr>
          <td>Issue number (<b>mandatory</b>)</td>
          <td>Date (<b>mandatory</b>) (<i>dd/mm/yyyy</i>)</td>
          <td>Hours (<b>mandatory</b>)</td>
          <td>Activity (if empty than use default)</td>
          <td>Comments</td>
        </tr>
      </tbody>
    </table>
  </p>
</div>
